<?php
header('location: ./hboard');
exit;
//error_reporting(E_ALL);

//chdir('./hboard');

//require './include/startup.php';

/*
// 모바일 기기로 접속시
if ( MOBILE_AGENT ) {
	if ( $_GET['mobile'] == 'off' ) {
		setcookie('mobile', 'off', 0, '/', COOKIE_DOMAIN);
	} else if ( $_COOKIE['mobile'] != 'off' ) {
		header('Location: '.URL_MOBILE);
		exit;
	}
}
*/

//$g->var['layout_head_title'] = 'HTLine';
//require './layout/default.php';
?>